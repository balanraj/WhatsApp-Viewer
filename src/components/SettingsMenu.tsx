export default function SettingsMenu({theme,setTheme,onClose}:{theme:string;setTheme:(x:string)=>void;onClose:()=>void}){return <div className="panel settings"><button className="close" onClick={onClose}>×</button><h2>Settings</h2><h3>Theme</h3>{['light','dark','system'].map(x=><label key={x}><input type="radio" checked={theme===x} onChange={()=>setTheme(x)}/>{x[0].toUpperCase()+x.slice(1)}</label>)}<p className="privacy">Your chat stays on your device. Nothing is uploaded to a server.</p></div>}
