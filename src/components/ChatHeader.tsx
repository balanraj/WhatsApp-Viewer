import {ArrowLeft,MoreVertical,Search,BarChart3,Settings} from 'lucide-react';export default function ChatHeader({name,count,onSearch,onStats,onSettings}:{name:string;count:number;onSearch:()=>void;onStats:()=>void;onSettings:()=>void}){return <header className="chat-header"><button><ArrowLeft/></button><div className="avatar">{name[0]?.toUpperCase()||'W'}</div><div className="head-title"><b>{name}</b><small>{count} participant{count===1?'':'s'}</small></div><div className="head-actions"><button onClick={onSearch}><Search/></button><button onClick={onStats}><BarChart3/></button><button onClick={onSettings}><Settings/></button><button><MoreVertical/></button></div></header>}
