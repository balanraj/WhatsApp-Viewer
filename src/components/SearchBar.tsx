import {X} from 'lucide-react';export default function SearchBar({value,setValue,count,onClose}:{value:string;setValue:(s:string)=>void;count:number;onClose:()=>void}){return <div className="searchbar"><input autoFocus value={value} onChange={e=>setValue(e.target.value)} placeholder="Search messages, people, files..."/><span>{value?`${count} result${count===1?'':'s'}`:''}</span><button onClick={onClose}><X/></button></div>}
